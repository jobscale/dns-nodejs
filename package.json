{
  "name": "dns-nodejs",
  "version": "0.2.10",
  "description": "dns, nameserver",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jobscale/dns-nodejs.git"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/jobscale/dns-nodejs/issues"
  },
  "homepage": "https://github.com/jobscale/dns-nodejs#readme",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "lint": "eslint",
    "pretest": "date -Iseconds",
    "posttest": "npm run lint",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest --runInBand --coverage --collectCoverage --verbose"
  },
  "pre-commit": "test",
  "jest": {
    "coveragePathIgnorePatterns": [
      "app/resolver.js"
    ]
  },
  "dependencies": {
    "@jobscale/logger": "^0.10.5",
    "dns-packet": "^5.6.1"
  },
  "devDependencies": {
    "@jobscale/eslint-plugin-standard": "^0.0.12",
    "eslint": "^10.0.0",
    "eslint-plugin-jest": "^29.15.0",
    "jest": "^30.2.0",
    "pre-commit": "^1.2.2"
  }
}
